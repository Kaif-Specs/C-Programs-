
    //vector<int>vec (5,0);//0 index value 5 size of the vector
    // with zero value vector<int>vec;
    //with some value vector<int>vec={1,2,3,4,5,6}
    //for(int i : vec){
       // cout<<i<<endl;
    //}
    
    //vector<char>vec = {'q','w','e','r','r','r','r','e','e','w',};
   
    /*vector<int>vec;
     cout<<"size="<<vec.size()<<endl;
    vec.push_back(20);
    vec.push_back(24);
    vec.push_back(27);
    vec.push_back(22);
     cout<<"after push back size="<<vec.size()<<endl;
     vec.pop_back();//deletd from the last
     cout<<vec.back()<<endl;
      cout<<vec.front()<<endl;
      cout<<vec.at(0)<<endl;
     for(int val : vec){
        cout<<val<<endl;
     }

    
    return 0;
}
    */
    /*vector<string> cars = {"Volvo","BMW","Ford","Mazda"};
    itvector<string>::iterator it;
    for(it = cars.begin(); it != cars.end(); ++it) 
    {
        cout << *it <<"\n";
    }
*/
#include <iostream>
#include <vector>
using namespace std;

int main() {
  /*vector<int> num {1, 2, 3, 4, 5};

  // declare iterator
  vector<int>::iterator iter;

  // initialize the iterator with the first element
  iter = num.begin();

// print the vector element
  cout << "num[0] = " << *iter << endl;

  // iterator points to the 3rd element
  iter = num.begin() + 2;
  cout << "num[2] = " << *iter;

  // iterator points to the last element
  iter = num.end() - 1;
  cout << "num[4] = " << *iter;

  return 0;
}
*/


